#include <Eigen/Geometry>

#include "window.h"
#include "scene.h"

using namespace std;
using namespace Eigen;

int main(int argc, char *argv[]) {
	//Scene scene;
	//Window window(scene);
	//window.show(argc, argv);
}

